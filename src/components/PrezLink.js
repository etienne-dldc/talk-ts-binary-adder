import styled from 'styled-components';

const PrezLink = styled.a({
  color: 'white',
  fontSize: '1.5em',
  fontWeight: 100,
  textDecoration: 'none',
});

export const HttpsPart = styled.span({
  opacity: 0.3,
});

export default PrezLink;
